__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/

# Engagement working area — runtime mount, never committed.
/work/

# Real secrets — only the .env.example template is committed.
.env

# Never commit audit logs, scan output, loot or scope files.
*.jsonl
*.log
/audit/
/loot/
/scope.txt
/results/
