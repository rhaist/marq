{
  "//": "LM Studio MCP config. In LM Studio: Program > Edit mcp.json, paste the",
  "//2": "'marq' entry below into your mcpServers, then adjust env values.",
  "//3": "The container talks to LM Studio over stdio (-i keeps stdin open).",
  "mcpServers": {
    "marq": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "--security-opt", "no-new-privileges:true",
        "--cap-drop", "ALL",
        "--cap-add", "NET_RAW",
        "--cap-add", "NET_ADMIN",
        "--cap-add", "NET_BIND_SERVICE",
        "-v", "marq-audit:/var/log/marq",
        "-e", "MARQ_OPERATOR",
        "-e", "MARQ_ENGAGEMENT",
        "-e", "MARQ_SCOPE",
        "-e", "MARQ_ALLOW_RAW_SHELL",
        "-e", "SHODAN_API_KEY",
        "-e", "CENSYS_API_ID",
        "-e", "CENSYS_API_SECRET",
        "-e", "GITHUB_TOKEN",
        "-e", "NUMVERIFY_API_KEY",
        "marq"
      ],
      "env": {
        "MARQ_OPERATOR": "your-name",
        "MARQ_ENGAGEMENT": "acme-webapp-2026-06",
        "MARQ_SCOPE": "*.example.com, 203.0.113.0/24 — per signed SOW",
        "MARQ_ALLOW_RAW_SHELL": "true",
        "//keys": "Optional — enrich OSINT tools. Leave blank to use keyless sources.",
        "SHODAN_API_KEY": "",
        "CENSYS_API_ID": "",
        "CENSYS_API_SECRET": "",
        "GITHUB_TOKEN": "",
        "NUMVERIFY_API_KEY": ""
      }
    }
  }
}
